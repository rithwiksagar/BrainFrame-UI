import { CopyButton } from "@/components/copybutton";


<div className="space-y-20">
<div>
### Step 1: Create a new Next.js project
<div className="not-prose bg-neutral-200 dark:bg-neutral-800 text-black dark:text-white/80 p-3 rounded-md flex items-center justify-between">
  <pre className="overflow-x-auto text-sm h-6 ">
    <code>npx create-next-app@latest my-app cd my-app</code>
  </pre>
  <CopyButton content="npx create-next-app@latest my-app" />
</div>
</div>

<div>
### Step 2: Install dependencies
<div className="not-prose bg-neutral-200 dark:bg-neutral-800 text-black dark:text-white/80 p-3 rounded-md flex items-center justify-between gap-4">
  <pre className="overflow-x-auto text-sm h-6">
    <code>npm install motion clsx tailwind-merge lucide-react</code>
  </pre>
  <CopyButton content="npm install motion clsx tailwind-merge lucide-react" />
</div>
</div>

<div>
### Step 3: Add utils file

Create a `utils.ts` file inside your `lib/` folder.

<div className="not-prose bg-neutral-200 dark:bg-neutral-800 text-black dark:text-white/80 p-3 rounded-md">
  <pre className="overflow-x-auto text-sm">
<code>{`import { clsx } from "clsx"
import { twMerge } from "tailwind-merge"
    export function cn(...inputs) {
    return twMerge(clsx(inputs))
    }`}</code>
  </pre>
</div>
</div>

### Step 3: Start using the component

<div className="not-prose bg-neutral-200 dark:bg-neutral-800 text-black dark:text-white/80 p-3 rounded-md">
  <pre className="overflow-x-auto text-sm">
    <code>
    {`import { PromptInput } from "@/components/ui/prompt-input"

export default function Page() {
  return <PromptInput />
}`}
</code>
</pre>
</div>

</div>