import { ModelContent, ModelInput, ModelItems, ModelSelector, ModelTrigger } from "@/components/ai/ModelSelector";
import  PreviewPage  from "@/components/ComponentPreviewCard"
import { DocsFooter } from "@/components/DocsFooter"
import ModelSelectorDemo from "@/app/docs/modelselector/ModelSelectorDemo"

### Model Selector

A customizable component for selecting and switching between AI models.

<ModelSelectorDemo />

<DocsFooter prevHref = "quick-start" prevTitle = "Installation" nextHref = "prompt-input" nextTitle = "Prompt Input"/>